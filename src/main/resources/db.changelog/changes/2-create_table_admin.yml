databaseChangeLog:
  - changeSet:
      id: 2
      author: kirrog
      changes:
        - createTable:
            columns:
              - column:
                  name: ID
                  type: UUID
                  remarks: id
                  constraints:
                    primaryKey: true
                    primaryKeyName: PK_ADMIN
                    nullable: false
                  autoIncrement: ${autoIncrement}
              - column:
                  name: USER_ID
                  type: UUID
                  remarks: fk to refer on table user
                  constraints:
                    - foreignKeyName: FK_USER_ID
                    - references: USER(ID)
            remarks: Admin
            tableName: admin
